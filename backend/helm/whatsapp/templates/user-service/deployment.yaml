apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "whatsapp.labels" . | nindent 4 }}
    {{- include "whatsapp.selectorLabels" (dict "name" "user-service" "root" .) | nindent 4 }}
spec:
  replicas: {{ .Values.services.userService.replicas }}
  selector:
    matchLabels:
      {{- include "whatsapp.selectorLabels" (dict "name" "user-service" "root" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "whatsapp.selectorLabels" (dict "name" "user-service" "root" .) | nindent 8 }}
    spec:
      containers:
        - name: user-service
          image: {{ include "whatsapp.serviceImage" (dict "root" . "image" .Values.services.userService.image) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.services.userService.httpPort }}
              protocol: TCP
            - name: grpc
              containerPort: {{ .Values.services.userService.grpcPort }}
              protocol: TCP
          env:
            - name: USER_POSTGRES_DSN
              value: "postgres://{{ .Values.postgres.user }}:{{ .Values.secrets.postgresPassword }}@postgres:{{ .Values.postgres.port }}/{{ .Values.postgres.database }}?sslmode=disable"
            - name: USER_REDIS_ADDR
              value: "redis:{{ .Values.redis.port }}"
            - name: USER_LOG_LEVEL
              value: {{ .Values.logLevel | quote }}
            - name: LOG_FORMAT
              value: {{ .Values.logFormat | quote }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            {{- toYaml .Values.services.userService.resources | nindent 12 }}
