apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "whatsapp.labels" . | nindent 4 }}
    {{- include "whatsapp.selectorLabels" (dict "name" "user-service" "root" .) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.services.userService.httpPort }}
      targetPort: http
      protocol: TCP
    - name: grpc
      port: {{ .Values.services.userService.grpcPort }}
      targetPort: grpc
      protocol: TCP
  selector:
    {{- include "whatsapp.selectorLabels" (dict "name" "user-service" "root" .) | nindent 4 }}
