apiVersion: v1
kind: Service
metadata:
  name: message-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "whatsapp.labels" . | nindent 4 }}
    {{- include "whatsapp.selectorLabels" (dict "name" "message-service" "root" .) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.services.messageService.httpPort }}
      targetPort: http
      protocol: TCP
    - name: grpc
      port: {{ .Values.services.messageService.grpcPort }}
      targetPort: grpc
      protocol: TCP
  selector:
    {{- include "whatsapp.selectorLabels" (dict "name" "message-service" "root" .) | nindent 4 }}
